// export function normalizeTicker(ticker: string): string {
//     return ticker
//       .replace(/[-_]/g, '')  // Remove dashes and underscores
//       .replace(/SWAP$/i, '') // Remove "SWAP" if present
//       .replace(/UMCBL$/i, '') // Remove Bitget suffix
//       .toUpperCase();        // Standardize casing
//   }
  